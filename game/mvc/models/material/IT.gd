
extends Building
func _init(age: int, isolation: int, surface: int, heating: bool, maintenance_agents_nb: int):
	# Appel au constructeur de la classe parent
	self._init(age, isolation, surface, heating, maintenance_agents_nb)
